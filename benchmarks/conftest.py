"""Sphinx Gallery hooks for the benchmarks gallery.

This file is intentionally minimal — Sphinx Gallery does not use
pytest fixtures, but its presence keeps the directory structure clean
so tools don't treat ``benchmarks/`` as a plain data directory.
"""

from __future__ import annotations
